{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn fastapi_modulo.modulos_sipet.modulo_base.runtime:app --host 0.0.0.0 --port ${PORT:-8080}'",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 120
  }
}
