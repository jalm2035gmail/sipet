{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "uvicorn fastapi_modulo.main:app --host 0.0.0.0 --port ${PORT}",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 120
  }
}
