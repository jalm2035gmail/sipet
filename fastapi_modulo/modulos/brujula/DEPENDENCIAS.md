## Dependencias detectadas

- `fastapi_modulo.main`: sigue siendo requerido por `brujula` para `SessionLocal`, resolución de tenant y renderizado del backend.
- `proyectando`: la dependencia directa fue eliminada. La lógica mínima de `datos_preliminares_store` y columnas de periodos fue copiada a `modelos/brujula_projection_store.py`.
