[options]
domain = localhost
db_host = localhost
db_port = 5432
db_user = sipet
db_password = cambia_esto
sqlite_db_path = /Users/jalm/Dropbox/Apps/SIPET/polobdtittlan.db
dbfilter = ^%h$
show_db_path = False

admin_passwd = cambia_esta_clave
list_db = False

proxy_mode = True
workers = 4
max_cron_threads = 2
log_level = info

[superadmin]
; Único usuario con acceso a /base_datos/inicializar
; Sobreescribibles con variables de entorno:
;   SYSTEM_SUPERADMIN_USERNAME, SYSTEM_SUPERADMIN_PASSWORD, SYSTEM_SUPERADMIN_EMAIL
superadmin_user = 0konomiyaki
superadmin_password = XX,$,26,sipet,26,$,XX
superadmin_email = alopez@avancoop.org
