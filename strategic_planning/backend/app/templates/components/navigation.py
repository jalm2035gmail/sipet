from typing import Dict, List


class BreadcrumbTemplate:
    """Ayuda a construir breadcrumbs consistentes."""

    @staticmethod
    def create(items: List[Dict[str, str]]) -> List[Dict[str, str]]:
        return items
